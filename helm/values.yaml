# Default values for job-observer.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: ghcr.io/akaimo/job-observer
  tag: 0.1.2
  pullPolicy: IfNotPresent

resources: {}

controller:
  replicas: 1
