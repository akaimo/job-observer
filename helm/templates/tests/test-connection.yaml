apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "job-observer.fullname" . }}-test-connection"
  labels:
    {{- include "job-observer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "job-observer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
